#include <stdio.h>
/**
 * main - A program that prints text followed by a new line using prinf
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
